<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    |
    | Here you may configure your settings for cross-origin resource sharing
    | or "CORS". This determines what cross-origin operations may execute
    | in web browsers. You are free to adjust these settings as needed.
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    /*
    |---------------------------------------------------------------------------
    | Allowed Methods
    |---------------------------------------------------------------------------
    |
    | Specify the HTTP methods that are allowed for CORS requests.
    | You can use ['*'] to allow all methods.
    |
    */

    'allowed_methods' => ['*'],

    /*
    |---------------------------------------------------------------------------
    | Allowed Origins
    |---------------------------------------------------------------------------
    |
    | Specify which origins are allowed to access your resources.
    | For development, you can set it to your frontend URL, например:
    | 'http://localhost:3000'
    | Для продакшена укажи свой домен.
    | Можно использовать ['*'] для всех.
    |
    */

    'allowed_origins' => ['http://localhost:5050'],

    /*
    |---------------------------------------------------------------------------
    | Allowed Origin Patterns
    |---------------------------------------------------------------------------
    |
    | Patterns to match allowed origins.
    | Можно оставить пустым, если не используешь.
    |
    */

    'allowed_origins_patterns' => [],

    /*
    |---------------------------------------------------------------------------
    | Allowed Headers
    |---------------------------------------------------------------------------
    |
    | Укажи заголовки, которые разрешены в запросах.
    | ['*'] разрешит все.
    |
    */

    'allowed_headers' => ['*'],

    /*
    |---------------------------------------------------------------------------
    | Exposed Headers
    |---------------------------------------------------------------------------
    |
    | Заголовки, которые браузер сможет читать из ответа.
    |
    */

    'exposed_headers' => [],

    /*
    |---------------------------------------------------------------------------
    | Max Age
    |---------------------------------------------------------------------------
    |
    | Время в секундах, на которое браузер может кешировать результат CORS проверки.
    | 0 — кеш не используется.
    |
    */

    'max_age' => 0,

    /*
    |---------------------------------------------------------------------------
    | Supports Credentials
    |---------------------------------------------------------------------------
    |
    | Разрешает отправку куков и авторизационных заголовков в кросс-доменных запросах.
    | Для Sanctum и сессий нужно ставить true.
    |
    */

    'supports_credentials' => true,

];
